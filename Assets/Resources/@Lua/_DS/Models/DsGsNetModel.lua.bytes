---- Ds连接Gs环境
local baseNetModel = require("_Common.BaseNetModel")
local _M = _MOE.class("DsGsNetModel", baseNetModel)
local MsgIds = require("_NetMsg.MsgId")


function _M:ConnectGS()
    local Session = _MOE.GameApp:GetGsDsSession()
    -- 连接GS
    _MOE.NetManager:ConnectServer(Session:GetGsIp(), Session:GetGsPort(), true)
end

local GSMsgProcess = {

}

setmetatable(_M.ReceiveMsgProcess, {__index = GSMsgProcess})

return _M